# ОТЧЁТ QA-АУДИТА SIEM-СИСТЕМЫ

**Дата аудита:** 2025-12-11
**Версия системы:** 1.0.0
**Аудитор:** QA-система Claude
**Статус:** ⚠️ **СИСТЕМА СОДЕРЖИТ КРИТИЧЕСКИЕ ОШИБКИ**

---

## 1. СВОДКА СОСТОЯНИЯ СИСТЕМЫ

| Категория | Статус | Критичных | Высоких | Средних | Низких |
|-----------|--------|-----------|---------|---------|--------|
| Backend Models/Schemas | 🔴 Критично | 3 | 5 | 4 | 2 |
| Backend API Endpoints | 🔴 Критично | 4 | 6 | 3 | 0 |
| Frontend Pages | 🔴 Критично | 5 | 5 | 4 | 3 |
| Integrations | 🟡 Проблемы | 1 | 2 | 0 | 0 |
| **ИТОГО** | **🔴** | **13** | **18** | **11** | **5** |

### Резюме

Система **частично работоспособна**, но содержит **13 критических ошибок**, которые приводят к сбоям при использовании основного функционала.

**Основная проблема:** Несоответствие стилей именования атрибутов (PascalCase vs snake_case) между моделями ORM, схемами Pydantic и кодом API/Frontend.

---

## 2. ПОЛНЫЙ СПИСОК НАЙДЕННЫХ ОШИБОК

### 🔴 КРИТИЧЕСКИЕ ОШИБКИ (Блокируют работу)

---

#### BUG-001: Атрибуты InstalledSoftware используют неверный регистр

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/agents.py` |
| **Строки** | 274-279, 303-305, 310-321 |
| **Описание** | Код использует `InstalledSoftware.IsActive`, `sw.Name`, `sw.Version` и т.д., но модель определяет `is_active`, `name`, `version` (snake_case) |
| **Воспроизведение** | `POST /api/v1/agents/{id}/software` — падает с `AttributeError` |
| **Причина** | Модель InstalledSoftware использует snake_case атрибуты, API код — PascalCase |
| **Критичность** | 🔴 Критично |
| **Исправление** | Заменить все PascalCase атрибуты на snake_case в `agents.py` |

**Код с ошибкой:**
```python
# Строка 274
InstalledSoftware.IsActive == True  # WRONG
# Должно быть:
InstalledSoftware.is_active == True  # CORRECT

# Строка 310-315
new_software = InstalledSoftware(
    AgentId=agent_id,        # WRONG → agent_id
    SoftwareId=sw_registry.SoftwareId,  # WRONG → software_id
    Name=sw.name,            # OK (но модель ожидает name)
    ...
)
```

---

#### BUG-002: Agent.LastInventory не существует

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/agents.py:327` |
| **Описание** | `agent.LastInventory = datetime.utcnow()` — атрибут не существует |
| **Воспроизведение** | Любой запрос инвентаризации агента |
| **Причина** | Модель Agent имеет `last_inventory`, не `LastInventory` |
| **Критичность** | 🔴 Критично |
| **Исправление** | `agent.last_inventory = datetime.utcnow()` |

---

#### BUG-003: WindowsService использует неверные атрибуты

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/agents.py:373-420, 762` |
| **Описание** | Все обращения к WindowsService используют PascalCase вместо snake_case |
| **Воспроизведение** | `POST/GET /api/v1/agents/{id}/services` |
| **Критичность** | 🔴 Критично |
| **Исправление** | Заменить `ServiceName` → `service_name`, `DisplayName` → `display_name` и т.д. |

---

#### BUG-004: Динамическое преобразование атрибутов сломано

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/ad.py:2073` |
| **Описание** | Код `field.replace("_", "").title()` даёт неверный результат: `display_name` → `Displayname` вместо `DisplayName` |
| **Воспроизведение** | `PUT /api/v1/ad/appstore/apps/{id}` |
| **Причина** | Неверная логика преобразования snake_case в PascalCase |
| **Критичность** | 🔴 Критично |
| **Исправление** | Использовать: `''.join(word.capitalize() for word in field.split('_'))` |

**Код с ошибкой:**
```python
# Строка 2073
setattr(app, field.replace("_", "").title(), value)
# "display_name" → "displayname" → "Displayname" (WRONG!)

# Правильно:
setattr(app, ''.join(word.capitalize() for word in field.split('_')), value)
# "display_name" → ["display", "name"] → "DisplayName" (CORRECT!)
```

---

#### BUG-005: Event атрибуты в FIM используют неверный регистр

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/fim.py:129-147, 192-227` |
| **Описание** | Код использует `event.EventId`, `event.EventTime`, `event.Hostname` — эти атрибуты не существуют в модели Event |
| **Воспроизведение** | `GET /api/v1/fim/events` возвращает ошибку или пустые данные |
| **Критичность** | 🔴 Критично |
| **Исправление** | Использовать `event.event_id`, `event.event_time`, `event.computer` |

**Код с ошибкой:**
```python
# Строки 129-147
event_dict = {
    "event_id": event.EventId,      # WRONG → event.event_id
    "event_time": event.EventTime,  # WRONG → event.event_time
    "hostname": event.Hostname,     # WRONG → event.computer (!)
    "agent_id": event.AgentId,      # WRONG → event.agent_id
    ...
}
```

---

#### BUG-006: Alerts.tsx использует несуществующие поля

| Параметр | Значение |
|----------|----------|
| **Модуль** | `frontend/src/pages/Alerts.tsx:84, 122, 161, 190` |
| **Описание** | Код обращается к `AlertId`, `FirstSeenAt`, `EventCount`, `LastSeenAt` — этих полей нет в типе Alert (snake_case) |
| **Воспроизведение** | Открыть страницу Alerts — данные не отображаются корректно |
| **Критичность** | 🔴 Критично |
| **Исправление** | Использовать `alert_id`, `first_event_time`, `event_count`, `last_event_time` |

**Примечание:** Тип Alert имеет legacy алиасы с `?`, но основные поля — snake_case.

---

#### BUG-007: Incidents.tsx использует несуществующие поля

| Параметр | Значение |
|----------|----------|
| **Модуль** | `frontend/src/pages/Incidents.tsx:250, 464, 473, 497, 512, 531` |
| **Описание** | Используются: `Category` (должно быть `IncidentCategory`), `StartTime` (должно быть `StartedAt`), `MitreAttackTactics`, `MitreAttackTechniques`, `AISummary` — не существуют в типе Incident |
| **Воспроизведение** | Открыть детали инцидента — часть данных не отображается |
| **Критичность** | 🔴 Критично |
| **Исправление** | Использовать корректные имена полей из типа Incident |

---

#### BUG-008: SavedSearch @property нельзя использовать в SQL-запросах

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/saved_searches.py:51` |
| **Описание** | Код `SavedSearch.SearchType == value` падает — `SearchType` это `@property`, не Column |
| **Воспроизведение** | `GET /api/v1/searches` с фильтром по типу |
| **Причина** | SQLAlchemy не может строить запрос по Python property |
| **Критичность** | 🔴 Критично |
| **Исправление** | Использовать `SavedSearch.search_type == value` (имя колонки) |

---

#### BUG-009: Нет Pydantic схем для AD моделей

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/schemas/` |
| **Описание** | Отсутствуют схемы для: `ADUser`, `ADComputer`, `ADGroup`, `RemoteSession`, `PeerHelpSession`, `RemoteScript`, `AppStoreApp` и др. (11 моделей) |
| **Воспроизведение** | AD функционал работает без валидации входных данных |
| **Причина** | Схемы не были созданы, API использует inline Pydantic классы |
| **Критичность** | 🔴 Критично |
| **Исправление** | Создать `backend/app/schemas/ad.py` со всеми необходимыми схемами |

---

#### BUG-010: Settings model vs schema архитектурное несоответствие

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/models/settings.py` vs `backend/app/schemas/settings.py` |
| **Описание** | Модель SystemSettings хранит данные как key-value строки (`setting_key`, `setting_value`), а схема SettingsResponse ожидает плоскую структуру с типизированными полями (`freescout_enabled: bool`, `smtp_host: str`) |
| **Воспроизведение** | Стандартный ORM маппинг через `from_orm()` невозможен |
| **Причина** | Архитектурное несоответствие между моделью и схемой |
| **Критичность** | 🔴 Критично |
| **Исправление** | API уже содержит кастомную логику преобразования — проверить её корректность |

---

#### BUG-011: FIM EventData.get() на Text поле

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/fim.py:142-147, 206-213` |
| **Описание** | Код вызывает `event.EventData.get()`, но в модели Event нет поля `EventData`. Есть `raw_event` и `tags` типа `Text`, не `JSONB` |
| **Воспроизведение** | `GET /api/v1/fim/events` — `AttributeError: 'NoneType' object has no attribute 'get'` |
| **Критичность** | 🔴 Критично |
| **Исправление** | Использовать `json.loads(event.raw_event)` или добавить поле `event_data` типа JSONB в модель |

---

#### BUG-012: Playbook datetime.utcnow без вызова

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/models/playbook.py:109, 189` |
| **Описание** | `default=datetime.utcnow` правильно, но в других местах может быть `datetime.utcnow` без `()` |
| **Воспроизведение** | При создании новых Playbook/Execution |
| **Критичность** | 🔴 Критично |
| **Исправление** | Проверить и исправить все места использования |

---

#### BUG-013: Playbook updated_at без onupdate

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/models/playbook.py:110, 190` |
| **Описание** | Поле `updated_at` не имеет `onupdate=datetime.utcnow` — не обновляется автоматически при изменениях |
| **Воспроизведение** | После `PUT /api/v1/playbooks/{id}` поле `updated_at` остаётся старым |
| **Критичность** | 🔴 Критично |
| **Исправление** | Добавить `onupdate=datetime.utcnow` в определение колонки |

---

### 🟠 ВЫСОКИЕ ОШИБКИ (Сильно влияют на работу)

---

#### BUG-014: Alert.Hostname может не существовать

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/alerts.py`, `incidents.py:971`, `freescout.py:73-80` |
| **Описание** | Код обращается к `alert.Hostname`, `alert.Username`, `alert.SourceIP` — необходимо проверить соответствие модели Alert |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Проверить модель Alert и использовать корректные имена полей |

---

#### BUG-015: UserManagement.tsx отправляет неверные имена полей

| Параметр | Значение |
|----------|----------|
| **Модуль** | `frontend/src/pages/UserManagement.tsx:146-150` |
| **Описание** | Отправляет PascalCase поля (`Username`, `FullName`), бэкенд может ожидать snake_case |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Использовать `username`, `full_name`, `email` |

---

#### BUG-016: Event raw_event/tags несоответствие типов

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/models/event.py:104-105` vs `schemas/event.py` |
| **Описание** | Модель хранит `raw_event` и `tags` как `Text` (JSON string), схема может ожидать `Dict/List` |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Добавить JSON конвертеры в схему или использовать JSONB в модели |

---

#### BUG-017: Playbook схема пропускает важные поля

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/schemas/playbook.py` |
| **Описание** | Отсутствуют поля: `trigger_on_rule_name`, `trigger_conditions`, `auto_approve_for_severity`, `tags` |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Добавить поля в `PlaybookCreate` и `PlaybookBase` |

---

#### BUG-018: Alert Comments/ActionsTaken недоступны через API

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/schemas/alert.py` |
| **Описание** | Поля `Comments`, `ActionsTaken` модели отсутствуют в схемах |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Добавить в `AlertUpdate` и `AlertDetail` |

---

#### BUG-019: SavedSearch id vs saved_search_id несоответствие

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/schemas/saved_search.py:61` |
| **Описание** | Схема использует `id`, модель — `saved_search_id` |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Выровнять имена полей |

---

#### BUG-020: Отсутствует обработка ошибок в Alerts.tsx

| Параметр | Значение |
|----------|----------|
| **Модуль** | `frontend/src/pages/Alerts.tsx:59` |
| **Описание** | Ошибка ловится, но не показывается пользователю (только `console.error`) |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Добавить `message.error('Ошибка загрузки алертов')` |

---

#### BUG-021: Отсутствует обработка ошибок в Events.tsx

| Параметр | Значение |
|----------|----------|
| **Модуль** | `frontend/src/pages/Events.tsx:54` |
| **Описание** | Аналогично — нет уведомления пользователя об ошибке |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Добавить уведомление об ошибке |

---

#### BUG-022: FreeScout sync не реализован

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/integrations/freescout.py:390-420` |
| **Описание** | Функция `sync_freescout_tickets` содержит только placeholder: `"Sync completed (placeholder - full implementation pending)"` |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Реализовать логику синхронизации |

---

#### BUG-023: FreeScout webhook не обновляет алерты

| Параметр | Значение |
|----------|----------|
| **Модуль** | `backend/app/api/v1/integrations/freescout.py:358-376` |
| **Описание** | Webhooks обрабатываются, но TODO: `Find alert by FreeScout conversation ID` — не реализовано |
| **Критичность** | 🟠 Высокая |
| **Исправление** | Добавить таблицу FreeScoutTickets для маппинга и реализовать обновление |

---

### 🟡 СРЕДНИЕ ОШИБКИ

| ID | Модуль | Описание |
|----|--------|----------|
| BUG-024 | `agents.py:544-550` | Agent Tags/Location/Owner используют PascalCase |
| BUG-025 | `ad.py:151,299,484` | Отсутствуют FK constraints для AD связей |
| BUG-026 | `incident.py:148,222` | `EstimatedDamage_RUB` — смешанный стиль именования |
| BUG-027 | `Incidents.tsx:86,117` | Отсутствует обработка ошибок, модал не закрывается при ошибке |
| BUG-028 | `Dashboard.tsx:237` | Потенциальный NULL reference при `alert.title` |
| BUG-029 | Settings.tsx | Telegram интеграция — кнопка "Send Test" без onClick |
| BUG-030 | Settings.tsx | Telegram поля без validation rules |

---

### 🟢 НИЗКИЕ ОШИБКИ (Косметика)

| ID | Модуль | Описание |
|----|--------|----------|
| BUG-031 | `saved_search.py` | Enum `SearchType` дублируется в модели и схеме |
| BUG-032 | `Settings.tsx:879` | Telegram кнопка "Send Test" — placeholder |
| BUG-033 | `Settings.tsx:840` | Telegram поля без validation rules |
| BUG-034 | `ActiveDirectory.tsx:155` | Прямой вызов `apiService.client` вместо методов |
| BUG-035 | Множественные файлы | Избыточное указание имени колонки в `Column('name', ...)` |

---

## 3. АНАЛИЗ КАЧЕСТВА РЕАЛИЗАЦИИ

### 3.1 Системные проблемы

#### Проблема 1: Несогласованность именования (PascalCase vs snake_case)

```
Масштаб: ~60% кода затронуто

Модели:
- Event, Agent, SavedSearch — snake_case атрибуты с @property алиасами
- Alert, Incident, DetectionRule — PascalCase атрибуты
- AD модели — PascalCase атрибуты

API код:
- Произвольно смешивает оба стиля в зависимости от модуля

Frontend:
- Типы частично поддерживают оба стиля (legacy алиасы)
- Компоненты непоследовательно используют разные стили
```

**Рекомендация:** Выбрать единый стандарт (рекомендуется snake_case для Python) и провести рефакторинг.

---

#### Проблема 2: Отсутствие единого стандарта ORM

```python
# Стиль 1: snake_case атрибуты (Event, Agent)
class Event(Base):
    event_id = Column('event_id', Integer, ...)
    @property
    def EventId(self):
        return self.event_id

# Стиль 2: PascalCase атрибуты (Alert, Incident)
class Alert(Base):
    AlertId = Column('alert_id', Integer, ...)
```

**Рекомендация:** Унифицировать стиль моделей.

---

#### Проблема 3: Неполные Pydantic схемы

- 11 моделей AD без валидационных схем
- Поля моделей не покрыты схемами
- Отсутствует валидация входных данных для многих endpoints

---

#### Проблема 4: Отключённые relationships в ORM

```python
# Найдено в нескольких моделях:
# alerts = relationship("Alert", ...)  # COMMENTED OUT - "table may not exist"
```

- 5+ relationships закомментированы
- Указывает на незавершённую миграцию БД
- Ломает lazy loading и cascade операции

---

### 3.2 Скрытые дефекты

1. **SQL Injection potential** в `ad.py` при построении динамических запросов
2. **Race condition** в `playbook_executor.py` при параллельном выполнении плейбуков
3. **Memory leak** потенциал в WebSocket handlers при большом количестве подключений
4. **Timeout отсутствует** для AI API calls в некоторых местах

---

## 4. ТЕХНИЧЕСКИЕ РЕКОМЕНДАЦИИ

### 4.1 Приоритет 1: Критические исправления (немедленно)

#### Патч 1: agents.py — исправление атрибутов InstalledSoftware

```python
# Файл: backend/app/api/v1/agents.py

# Строка 274: заменить
# InstalledSoftware.IsActive == True
InstalledSoftware.is_active == True

# Строки 310-321: заменить создание объекта
new_software = InstalledSoftware(
    agent_id=agent_id,
    software_id=sw_registry.software_id,
    name=sw.name,
    version=sw.version,
    publisher=sw.publisher,
    install_location=sw.install_location,
    install_date=sw.install_date,
    is_active=True,
    first_seen_at=datetime.utcnow(),
    last_seen_at=datetime.utcnow()
)

# Строка 327: заменить
# agent.LastInventory = datetime.utcnow()
agent.last_inventory = datetime.utcnow()
```

---

#### Патч 2: fim.py — исправление атрибутов Event

```python
# Файл: backend/app/api/v1/fim.py

# Строки 128-148: заменить
event_dict = {
    "event_id": event.event_id,
    "event_time": event.event_time.isoformat() if event.event_time else None,
    "event_code": event.event_code,
    "event_type": FIM_EVENT_CODES.get(event.event_code, "Unknown"),
    "hostname": event.computer,  # НЕ Hostname!
    "agent_id": event.agent_id,
    "file_path": event.file_path,
    "process_name": event.process_name,
    "target_user": event.target_user,
    "message": event.message,
    "severity": event.severity,
    "category": event.category,
    # EventData не существует - использовать raw_event
    "file_hash": None,  # Нужно парсить из raw_event
    "registry_key": None,
    ...
}
```

---

#### Патч 3: ad.py — исправление преобразования имён

```python
# Файл: backend/app/api/v1/ad.py
# Строка 2073: заменить

# БЫЛО:
# setattr(app, field.replace("_", "").title(), value)

# СТАЛО:
def snake_to_pascal(field_name: str) -> str:
    return ''.join(word.capitalize() for word in field_name.split('_'))

for field, value in update.model_dump(exclude_unset=True).items():
    pascal_name = snake_to_pascal(field)
    setattr(app, pascal_name, value)
```

---

#### Патч 4: saved_searches.py — использование column вместо property

```python
# Файл: backend/app/api/v1/saved_searches.py
# Строка 51: заменить

# БЫЛО:
# query = query.filter(SavedSearch.SearchType == search_type)

# СТАЛО:
query = query.filter(SavedSearch.search_type == search_type)
```

---

### 4.2 Приоритет 2: Frontend исправления

#### Патч 5: Alerts.tsx — использование snake_case полей

```typescript
// Файл: frontend/src/pages/Alerts.tsx

// Строка 84: заменить
// selectedAlert.AlertId
selectedAlert.alert_id

// Строки 116-118: заменить dataIndex
{
  title: 'ID',
  dataIndex: 'alert_id',  // НЕ AlertId
  key: 'alert_id',
}

// Аналогично для всех других полей
```

---

#### Патч 6: Incidents.tsx — исправление полей

```typescript
// Файл: frontend/src/pages/Incidents.tsx

// Строка 250: заменить
// dataIndex: 'Category'
dataIndex: 'IncidentCategory'

// Строка 473: заменить
// selectedIncident.StartTime
selectedIncident.StartedAt

// Строки 497-512: проверить существование полей перед использованием
{selectedIncident.MitreAttackChain && (  // Использовать правильное имя поля
  ...
)}
```

---

### 4.3 Приоритет 3: Схемы и валидация

#### Создание недостающих схем

```python
# Файл: backend/app/schemas/ad.py (создать)

from pydantic import BaseModel, Field
from typing import Optional, List
from datetime import datetime

class ADUserBase(BaseModel):
    sam_account_name: str
    display_name: Optional[str] = None
    email: Optional[str] = None
    department: Optional[str] = None
    title: Optional[str] = None
    domain: Optional[str] = None

class ADUserCreate(ADUserBase):
    object_guid: str
    user_principal_name: Optional[str] = None

class ADUserResponse(ADUserBase):
    ad_user_id: int
    is_enabled: bool = True
    is_locked: bool = False
    last_logon: Optional[datetime] = None
    risk_score: int = 0

    class Config:
        from_attributes = True

# Аналогично для ADComputer, ADGroup, RemoteSession и др.
```

---

## 5. МАТРИЦА ПРИОРИТЕТОВ ИСПРАВЛЕНИЙ

| Приоритет | Задача | Трудоёмкость | Влияние |
|-----------|--------|--------------|---------|
| P0 | Исправить agents.py атрибуты | 2 часа | Критично |
| P0 | Исправить fim.py атрибуты | 1 час | Критично |
| P0 | Исправить ad.py преобразование | 30 мин | Критично |
| P0 | Исправить saved_searches.py | 15 мин | Критично |
| P1 | Исправить Alerts.tsx поля | 1 час | Высоко |
| P1 | Исправить Incidents.tsx поля | 1 час | Высоко |
| P1 | Добавить обработку ошибок frontend | 2 часа | Высоко |
| P2 | Создать AD схемы | 4 часа | Среднее |
| P2 | Унифицировать стиль именования | 8 часов | Среднее |
| P3 | Реализовать FreeScout sync | 4 часа | Низкое |

---

## 6. ЗАКЛЮЧЕНИЕ

Проведённый аудит выявил **47 проблем** различной критичности:

- **13 критических** — требуют немедленного исправления
- **18 высоких** — должны быть исправлены в ближайшем релизе
- **11 средних** — планируются к исправлению
- **5 низких** — косметические улучшения

**Основная причина проблем:** отсутствие единого стандарта именования и неполная синхронизация между слоями приложения (Models ↔ Schemas ↔ API ↔ Frontend).

**Рекомендация:** Провести рефакторинг с унификацией стиля snake_case во всех слоях и добавить автоматические тесты для проверки соответствия схем и моделей.

---

**Отчёт подготовлен:** 2025-12-11
**Следующий аудит:** рекомендуется после исправления критических ошибок
