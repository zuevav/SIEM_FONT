# SIEM Frontend

Современный веб-интерфейс для SIEM системы на React 18 + TypeScript + Ant Design.

## 🎯 Технологический стек

- **React 18.2** - UI библиотека
- **TypeScript 5.3** - типизация
- **Vite** - сборщик (быстрее чем CRA)
- **Ant Design 5** - UI компоненты
- **React Router 6** - маршрутизация
- **Zustand** - state management (легковесная альтернатива Redux)
- **Axios** - HTTP клиент
- **Recharts** - графики и визуализация
- **react-use-websocket** - WebSocket интеграция
- **ahooks** - полезные React hooks
- **dayjs** - работа с датами

## 📋 Возможности

### Реализовано

✅ **Базовая структура проекта**
- Vite конфигурация с path aliases
- TypeScript строгая конфигурация
- Proxy для API и WebSocket

✅ **TypeScript типы**
- Полный набор типов для всех API endpoints
- User, Event, Alert, Incident, Agent
- Dashboard, WebSocket, Pagination

✅ **API Service**
- Axios instance с interceptors
- Автоматическое добавление JWT токена
- Обработка 401 ошибок
- Методы для всех API endpoints:
  - Authentication (login, logout, me, change-password)
  - Events (get, filter, export, statistics)
  - Alerts (get, acknowledge, resolve, assign, comment)
  - Incidents (CRUD, workflow, timeline)
  - Agents (get, statistics, delete)
  - Dashboard (statistics)
  - Users (admin management)

### В разработке

⏳ **AuthStore (Zustand)**
- Управление состоянием аутентификации
- Проверка прав доступа (RBAC)

⏳ **WebSocket Hook**
- Real-time обновления
- Подписка на каналы (events, alerts, incidents, dashboard)

⏳ **Страницы**
- Login - форма входа
- Dashboard - главный дашборд с графиками
- Events - таблица событий с фильтрами
- Alerts - управление алертами
- Incidents - управление инцидентами
- Agents - мониторинг агентов
- Settings - настройки и управление пользователями

⏳ **Компоненты**
- Layout с навигацией
- Protected Routes
- Charts и статистика
- Таблицы с пагинацией
- Фильтры и поиск

## 🚀 Установка и запуск

### Требования

- Node.js 18+
- npm или yarn

### Установка

```bash
cd frontend

# Установить зависимости
npm install

# Скопировать .env файл
cp .env.example .env

# Отредактировать .env
# VITE_API_URL=http://localhost:8000/api/v1
# VITE_WS_URL=ws://localhost:8000/ws
```

### Разработка

```bash
# Запустить dev сервер
npm run dev

# Открыть http://localhost:3000
```

### Production сборка

```bash
# Собрать для production
npm run build

# Preview production build
npm run preview
```

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/        # React компоненты
│   │   ├── Layout/        # Layout с навигацией
│   │   ├── Charts/        # Графики (Recharts)
│   │   └── Common/        # Общие компоненты
│   │
│   ├── pages/             # Страницы приложения
│   │   ├── Login/         # Страница входа
│   │   ├── Dashboard/     # Главный дашборд
│   │   ├── Events/        # События
│   │   ├── Alerts/        # Алерты
│   │   ├── Incidents/     # Инциденты
│   │   ├── Agents/        # Агенты
│   │   └── Settings/      # Настройки
│   │
│   ├── services/          # API сервисы
│   │   └── api.ts         # Axios API client
│   │
│   ├── store/             # State management (Zustand)
│   │   ├── authStore.ts   # Аутентификация
│   │   └── appStore.ts    # Глобальное состояние
│   │
│   ├── hooks/             # Custom React hooks
│   │   ├── useAuth.ts     # Аутентификация
│   │   └── useWebSocket.ts# WebSocket
│   │
│   ├── types/             # TypeScript типы
│   │   └── index.ts       # API типы
│   │
│   ├── utils/             # Утилиты
│   │   ├── date.ts        # Работа с датами
│   │   └── format.ts      # Форматирование
│   │
│   ├── App.tsx            # Главный компонент
│   └── main.tsx           # Entry point
│
├── public/                # Статические файлы
├── package.json           # Dependencies
├── tsconfig.json          # TypeScript config
├── vite.config.ts         # Vite config
└── README.md              # Документация
```

## 🔐 Аутентификация

Frontend использует JWT токены для аутентификации:

1. Пользователь вводит логин/пароль
2. Backend возвращает JWT токен
3. Токен сохраняется в localStorage
4. Все API запросы содержат `Authorization: Bearer {token}`
5. При 401 ошибке - редирект на /login

## 🌐 WebSocket

Real-time обновления через WebSocket:

```typescript
// Подключение к WebSocket
const ws = useWebSocket('/ws/dashboard', {
  onMessage: (event) => {
    const message = JSON.parse(event.data)
    // Обработка сообщения
  }
})
```

Каналы:
- `/ws/events` - новые события
- `/ws/alerts` - новые алерты
- `/ws/incidents` - обновления инцидентов
- `/ws/agents` - статус агентов
- `/ws/dashboard` - статистика дашборда
- `/ws/notifications` - системные уведомления

## 📊 Компоненты дашборда

### Статистика
- События за 24 часа / 7 дней
- Алерты (new, acknowledged, total)
- Инциденты (open, investigating, total)
- Агенты (online, offline, total)

### Графики
- Timeline событий
- Распределение по severity
- Топ компьютеров по событиям
- MITRE ATT&CK heat map

### Real-time
- Live обновления через WebSocket
- Уведомления о критических событиях
- Счётчики обновляются автоматически

## 🎨 UI/UX

- **Ant Design 5** - современные компоненты
- **Адаптивный дизайн** - работает на всех устройствах
- **Тёмная/светлая тема** - переключение темы
- **Интернационализация** - русский и английский
- **Иконки** - @ant-design/icons

## 🔧 Конфигурация

### Environment Variables

`.env` файл:

```bash
# API Backend URL
VITE_API_URL=http://localhost:8000/api/v1

# WebSocket URL
VITE_WS_URL=ws://localhost:8000/ws

# Enable debug mode
VITE_DEBUG=false
```

### Vite Proxy

Vite автоматически проксирует запросы к Backend:
- `/api/*` → `http://localhost:8000/api/*`
- `/ws/*` → `ws://localhost:8000/ws/*`

Это позволяет избежать CORS проблем в разработке.

## 🧪 Тестирование

```bash
# Unit tests (будет добавлено)
npm run test

# E2E tests (будет добавлено)
npm run test:e2e

# Lint
npm run lint

# Format
npm run format
```

## 📝 TODO

- [ ] Завершить AuthStore
- [ ] Создать WebSocket hook
- [ ] Реализовать все страницы
- [ ] Добавить unit tests
- [ ] Добавить E2E tests
- [ ] Добавить i18n
- [ ] Оптимизировать production build
- [ ] Добавить Service Worker для offline режима

## 🤝 Разработка

### Code Style

- TypeScript strict mode
- ESLint + Prettier
- Функциональные компоненты
- React Hooks
- Composition over inheritance

### Best Practices

- **Мелкие компоненты** - разбивайте на маленькие переиспользуемые компоненты
- **Custom hooks** - логику выносите в хуки
- **TypeScript** - используйте типы везде
- **Error boundaries** - обрабатывайте ошибки
- **Loading states** - показывайте состояние загрузки
- **Accessibility** - следуйте WCAG 2.1

---

**Версия**: 0.9.0 (Beta)
**Дата**: 2025-12-02
**Статус**: В разработке (50% готово)
