# Настройка AI-провайдера для SIEM

## 🤖 Доступные AI-провайдеры

SIEM система поддерживает несколько AI-провайдеров для анализа событий безопасности:

### 1. **DeepSeek (Рекомендуется)** 🌟
- ✅ **Бесплатный** или очень доступный
- ✅ Высокое качество анализа
- ✅ OpenAI-совместимый API
- ✅ Быстрая регистрация
- 🌐 [https://platform.deepseek.com](https://platform.deepseek.com)

### 2. **Yandex GPT**
- ⚠️ Требуется Yandex Cloud аккаунт (платно)
- ✅ Хорошая поддержка русского языка
- 🌐 [https://cloud.yandex.ru/services/yandexgpt](https://cloud.yandex.ru/services/yandexgpt)

---

## 📝 Инструкция по настройке DeepSeek (рекомендуется)

### Шаг 1: Регистрация

1. Перейдите на [https://platform.deepseek.com](https://platform.deepseek.com)
2. Нажмите **Sign Up** (Регистрация)
3. Зарегистрируйтесь через email или GitHub
4. Подтвердите email

### Шаг 2: Получение API ключа

1. После входа перейдите в раздел **API Keys**
2. Нажмите **Create API Key**
3. Введите название ключа (например, "SIEM System")
4. Скопируйте полученный ключ (он начинается с `sk-`)
5. ⚠️ **Сохраните ключ** - он больше не будет показан!

### Шаг 3: Настройка SIEM

Откройте файл `.env` в корне проекта и добавьте:

```bash
# ============================================================================
# AI PROVIDER
# ============================================================================
AI_PROVIDER=deepseek

# DeepSeek Configuration
DEEPSEEK_ENABLED=true
DEEPSEEK_API_KEY=sk-ваш-ключ-здесь
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_TEMPERATURE=0.3
DEEPSEEK_MAX_TOKENS=2000
```

### Шаг 4: Проверка

Запустите backend:
```bash
cd backend
python -m uvicorn app.main:app --reload
```

В логах вы должны увидеть:
```
✓ DeepSeek AI provider initialized
```

---

## 📝 Инструкция по настройке Yandex GPT (альтернатива)

### Шаг 1: Создание аккаунта Yandex Cloud

1. Перейдите на [https://cloud.yandex.ru](https://cloud.yandex.ru)
2. Войдите с Yandex ID или создайте аккаунт
3. Привяжите банковскую карту (для оплаты)
4. Создайте каталог (folder)

### Шаг 2: Получение API ключа

1. Перейдите в **IAM** → **Сервисные аккаунты**
2. Создайте сервисный аккаунт с ролью `ai.languageModels.user`
3. Создайте **API ключ** для сервисного аккаунта
4. Скопируйте ключ (формат: `AQVN...`)
5. Скопируйте **ID каталога** (folder_id)

### Шаг 3: Настройка SIEM

Откройте файл `.env` и добавьте:

```bash
# ============================================================================
# AI PROVIDER
# ============================================================================
AI_PROVIDER=yandex_gpt

# Yandex GPT Configuration
YANDEX_GPT_ENABLED=true
YANDEX_GPT_API_KEY=AQVN...ваш-ключ-здесь
YANDEX_GPT_FOLDER_ID=b1g...ваш-folder-id
YANDEX_GPT_MODEL=yandexgpt-lite
YANDEX_GPT_TEMPERATURE=0.3
YANDEX_GPT_MAX_TOKENS=2000
```

---

## 🔄 Переключение между провайдерами

Чтобы переключиться между провайдерами, измените в `.env`:

```bash
# Использовать DeepSeek (бесплатно)
AI_PROVIDER=deepseek

# ИЛИ использовать Yandex GPT (платно)
AI_PROVIDER=yandex_gpt
```

Система автоматически выберет настроенный провайдер при запуске.

---

## ⚙️ Дополнительные настройки

### Параметры AI-анализа

```bash
# Размер батча для обработки событий
AI_BATCH_SIZE=10

# Интервал автоматической обработки (секунды)
AI_PROCESS_INTERVAL_SEC=60

# Количество попыток при ошибке
AI_RETRY_ATTEMPTS=3

# Использовать правила детекции при ошибке AI
AI_FALLBACK_TO_RULES=true
```

### Настройка температуры (креативность)

**Temperature** (0.0 - 1.0):
- `0.0-0.3` - Строгий, детерминированный (для отчётов CBR)
- `0.3-0.5` - Сбалансированный (рекомендуется)
- `0.5-1.0` - Креативный (не рекомендуется для security анализа)

```bash
DEEPSEEK_TEMPERATURE=0.3
YANDEX_GPT_TEMPERATURE=0.3
```

### Настройка max_tokens (длина ответа)

```bash
# Для коротких анализов
DEEPSEEK_MAX_TOKENS=1000

# Для детальных анализов и отчётов (по умолчанию)
DEEPSEEK_MAX_TOKENS=2000

# Для очень подробных отчётов
DEEPSEEK_MAX_TOKENS=4000
```

---

## 💰 Стоимость использования

### DeepSeek
- **Бесплатный уровень**: Да (ограниченное количество запросов)
- **Платный уровень**: ~$0.14 за 1M входных токенов, ~$0.28 за 1M выходных
- **Примерная стоимость**: $10-20/месяц для средней нагрузки
- **Модели**: deepseek-chat, deepseek-coder

### Yandex GPT
- **Бесплатный уровень**: Нет
- **YandexGPT Lite**: ~1₽ за 1000 токенов (~$0.011)
- **YandexGPT Pro**: ~8₽ за 1000 токенов (~$0.088)
- **Примерная стоимость**: 3,000-10,000₽/месяц (~$33-110)

### Рекомендация
✅ **Начните с DeepSeek** - бесплатный/доступный, отличное качество

---

## 🧪 Тестирование AI-провайдера

### Через API

```bash
# Получить токен
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "Admin123!"}'

# Протестировать анализ события (когда будет реализовано)
curl -X POST http://localhost:8000/api/v1/events/analyze \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "event_id": 1
  }'
```

### Через Python

```python
from app.services import get_ai_service

# Получить настроенный провайдер
ai = get_ai_service()

# Проверить, что провайдер включен
print(f"AI provider: {ai.get_provider_name()}")
print(f"Is enabled: {ai.is_enabled()}")

# Протестировать анализ
event_data = {
    "source_type": "Windows Security",
    "event_code": 4625,
    "severity": 3,
    "message": "Failed login attempt",
    "subject_user": "Administrator"
}

result = await ai.analyze_event(event_data)
print(result)
```

---

## ❗ Troubleshooting

### Ошибка: "No AI provider is configured"

**Проблема**: Не настроен ни один AI провайдер

**Решение**:
1. Проверьте файл `.env`
2. Убедитесь, что `DEEPSEEK_ENABLED=true` или `YANDEX_GPT_ENABLED=true`
3. Проверьте, что указан `API_KEY`

### Ошибка: "DeepSeek API error: 401"

**Проблема**: Неверный API ключ

**Решение**:
1. Проверьте правильность ключа в `.env`
2. Убедитесь, что ключ начинается с `sk-`
3. Создайте новый ключ на [platform.deepseek.com](https://platform.deepseek.com)

### Ошибка: "Yandex GPT API error: 403"

**Проблема**: Недостаточно прав или неверный folder_id

**Решение**:
1. Проверьте роль сервисного аккаунта (`ai.languageModels.user`)
2. Проверьте правильность `YANDEX_GPT_FOLDER_ID`
3. Убедитесь, что есть средства на балансе Yandex Cloud

### AI анализ не работает

**Проверьте**:
1. Backend запущен без ошибок
2. В логах есть строка "✓ AI provider initialized"
3. Провайдер отвечает (проверьте через curl или браузер)

**Логи**:
```bash
# Просмотр логов
tail -f backend/logs/app.log

# Проверка конфигурации
grep AI_ .env
```

---

## 📚 Дополнительные ресурсы

### DeepSeek
- 📖 [Документация API](https://platform.deepseek.com/api-docs)
- 💬 [Discord сообщество](https://discord.gg/deepseek)
- 🐛 [GitHub Issues](https://github.com/deepseek-ai)

### Yandex GPT
- 📖 [Документация](https://cloud.yandex.ru/docs/yandexgpt)
- 💬 [Telegram чат](https://t.me/yandex_cloud)
- 📧 [Поддержка](https://console.cloud.yandex.ru/support)

---

## 🔐 Безопасность

### Защита API ключей

⚠️ **Никогда не коммитьте `.env` в Git!**

```bash
# Убедитесь, что .env в .gitignore
echo ".env" >> .gitignore

# Используйте переменные окружения
export DEEPSEEK_API_KEY="sk-ваш-ключ"

# Или храните в защищённом хранилище (Vault, Secret Manager)
```

### Ротация ключей

Рекомендуется менять API ключи каждые 90 дней:

1. Создайте новый ключ
2. Обновите `.env`
3. Перезапустите backend
4. Удалите старый ключ

---

## ✅ Что дальше?

После настройки AI-провайдера система автоматически будет:

1. ✅ Анализировать события безопасности
2. ✅ Классифицировать угрозы (score 0-100)
3. ✅ Предоставлять рекомендации по алертам
4. ✅ Создавать детальные анализы инцидентов
5. ✅ Генерировать отчёты для ЦБ РФ

**Готово!** 🎉
